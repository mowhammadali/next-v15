import React from 'react'

const ProductIdLayout = ({children}: {children: React.ReactNode}) => {
  return (
    <div>
        <section>ProductId Layout</section>
        {children}
    </div>
  )
}

export default ProductIdLayout